// #include <types.hpp>


// namespace tetris
// {
//   coord::coord() noexcept : x {0}, y {0} {}
//   coord::coord(Float x, Float y) noexcept : x {x}, y {y} {}
//   coord coord::operator+(const coord & other) noexcept 
//   { return coord(x+other.x, y+other.y); }

//   coord coord::operator-(const coord & other) noexcept
//   { return coord(x-other.x, y-other.y); }

//   coord coord::operator*(const std::array<Float, 4> mat) noexcept 
//   { return coord(y, -x); }

//   coord coord::operator*(const Float mpl) noexcept 
//   { return coord(mpl * x, mpl * y); }
  
// } // namespace tetris
